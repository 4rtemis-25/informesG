import { VentasDirective } from './ventas.directive';

describe('VentasDirective', () => {
  it('should create an instance', () => {
    const directive = new VentasDirective();
    expect(directive).toBeTruthy();
  });
});
